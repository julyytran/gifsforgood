<div class="container">
  <div class="gifs">
   <div class="row text-center">
     <h1>top_items</h1>
    <% @gifs.each do |gif| %>
    <%= render partial: './gif_column', locals: { gif: gif } %>
    <% end %>
    </div>
  </div>
</div>
